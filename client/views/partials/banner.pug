.container
  if user && user.username
    .row
      .col-md-12
        .well
          h1 Yer Account
          h3 Hello, #{user.username}
          p.small User Id: #{user.id}
          p.small Email: #{user.email}
  block content